# T5.2 seed matrix provenance refresh

Current causal seed-memory runtime is integrated, but `SEED_CONSUMER_MATRIX.json` still carries the generation-time `baseCommit` `adf1bffa7298bff6d7cebab88a3388c559cd3588`.

That field is stale relative to the reconstruction and subsequent integrations. The matrix must not be treated as authoritative closure evidence until it is regenerated by the official/current generator on the exact `main` used for closure work.

Required refresh:

1. start from the then-current `main`;
2. run the official seed consumer matrix generator/audit, do not hand-edit generated rows;
3. preserve the structural invariants: 210 catalog seeds, live vs historical separation, owner coverage, payload/scope evidence and exactly the owner-backed Codex-ready consumers actually supported by that head;
4. update generated baseline/provenance metadata to that exact commit;
5. run focused T5.2 tests and full Repository Integrity on the final head;
6. remove `.provenance-refresh-required` only in the same commit/PR that contains the regenerated matrix and green evidence.

This is a provenance/evidence correction. It does not authorize changes to EVENTS, contentIdentity, seed definitions, save schema, RNG, scheduler, market or NPC semantics.
