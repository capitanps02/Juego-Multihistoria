# Conditional canon status — 23–30

Historical canonical inventory:

- 23–26: 20 callbacks;
- 26–30: 24 callbacks;
- total Agent-6 conditional ownership: 44.

The T5.1 field reviews are explicit: exact runtime IDs/titles do not certify semantics. The generic conditional factories must be replaced scene by scene and same-ID pending content needs content-identity migration.

## 23–26 strongest condition cores

### CEVT_24_CHAT_01 — La captura existe

Status: `codex_ready` for **gate/history reconstruction**, not yet safe for final scene copy.

Evidence already in runtime:

- producer: `EVT_24_LOCK_001`;
- memory: `SEED_PRIVATE_CHAT`;
- scope: `origin_club`, with a dedicated T5.2 scope proof;
- producer payload preserves the actual prior choice (`A/B/C/D`), corresponding to criticise / lower the tone / write nothing / leave the group.

The canonical review says the callback must resolve whether the player actually wrote or stayed silent. Do not reduce this to seed presence alone.

Blocker before implementation: the compressed canonical matrix containing the exact callback scene/choice contract is not available through the UTF-8 GitHub connector. Do not invent replacement choices from the title. Once exact source text is available, this should be one of the first conditional rewrites.

### CEVT_25_BODY_02 — Recaída sin culpable

Status: `blocked_shared_authority` at the declarative gate layer.

Useful evidence exists:

- high body load;
- `SEED_LOAD_MANAGEMENT`;
- `EVT_23_BODY_001` persists `payload.plan` with exact strategies (`weekly_prevention`, `full_load_until_pain`, `post_full_match`, `external_review_first`).

Canon requires high load **after returning/following protocol**. The current `facts.*` projection does not expose the load-management plan, so a simple `HAS_SEED_LOAD_MANAGEMENT` gate would incorrectly admit the full-load route. Seed-memory owner should expose a scope-valid scalar fact such as `facts.loadManagementPlan` (or an equivalent certified protocol boolean) before wiring this callback.

### CEVT_23_CLARA_03

Status: `blocked_shared_authority` / partial. Current national-selection route is represented; transfer route and exact scene semantics still require review. Aggregate national authority is not a concrete list/call-up unless the event contract only needs historical aggregate status.

### CEVT_24_OWNER_02

Status: `blocked_shared_authority`. `CLUB_OWNER_CHANGE` plus profile is not enough until ownership change is scoped to the protagonist's current club and its actual current institutional actors.

## Non-negotiable conditional rules

- A seed contextualizes an external event; it must not invent that event.
- Same string ID does not make old generic A/B/C pending content compatible with a canonical rewrite.
- Preserve historical callback history; migrate only pending content according to content identity.
- `origin_club` and `origin_season` scope must be honored before a callback can consume a seed.
- Do not infer third-party injuries, transfers, careers, selection presence, coach dismissal or business losses from protagonist memory alone.
