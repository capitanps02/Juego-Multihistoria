# Conditional canon status — 23–30

Agent-6 finite inventory:

- 23–26: **20/20 owner-side resolved**;
- 26–30: **24/24 owner-side resolved**;
- total: **44/44**.

“Resolved” here means scene-specific executable runtime semantics exist, or every A6-owned part exists and the event fails closed on a named external-authority fact. It does not mean A0 has activated the contentIdentity generation.

The old generic conditional factory is not certification and is not used to count these 44.

## Causal contracts that must survive integration

### CEVT_24_CHAT_01 — La captura existe

The staged callback reads `SEED_PRIVATE_CHAT` but **does not** treat seed presence as the remembered action. Its outcome modifiers require the exact prior choice fact:

- `facts.privateChatChoice = A` — criticised;
- `B` — lowered the tone;
- `C` — wrote nothing;
- `D` — left the group.

A2 PR #217 is the owner of the scope-valid projection. Until that projection is on the integrated branch, the callback remains fail-closed.

### CEVT_25_BODY_02 — Recaída sin culpable

The callback requires both a factual recurrence and the exact load-management plan. It distinguishes:

- `weekly_prevention`;
- `full_load_until_pain`;
- `post_full_match`;
- `external_review_first`.

A2 PR #217 owns the `SEED_LOAD_MANAGEMENT.payload.plan` projection. Presence of `HAS_SEED_LOAD_MANAGEMENT` alone is insufficient.

## External-event discipline

The remaining callbacks preserve these rules:

- seed memory may contextualize an external event, never create it;
- third-party injury/performance requires explicit sporting authority;
- business loss requires factual finance/business result;
- coach/owner/agent identity must be current-club/current-representation scoped;
- tournament omission/current squad role is not inferred from caps or national standing;
- market interest/offers are not inferred from `marketHeat`;
- same ID does not make an old generic pending callback content-compatible with the canonical rewrite.

A0 owns contentIdentity migration for pending callbacks. A6 owns no parallel generation chain.
