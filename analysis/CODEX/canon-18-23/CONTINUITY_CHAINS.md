# CONTINUITY CHAINS — 18–23

Format: producer → decision/fact → memory → consumer → consequence.

## Closed / executable

### Bruno
`EVT_18_TEAM_001` → HELP_REASONABLE / NO_PROMISE / AGREE_BUT_SELF / TELL_MENA → `SEED_BRUNO_FAVOR.payload.stance` → `CEVT_18_BRUNO_01` → live stance changes later outcome weights. `helped/no_promise` are not equivalent to `betrayed/code_broken/refused`; terminal seed does not act live.

### Coach / Mena
`EVT_18_PRE_003` → tactical response → `SEED_MENA_EARLY_READ.read` + public coach decisions → `SEED_COACH_PUBLIC.stance` → `CEVT_18_CCH_01` → Mena-facing reception changes. No unrelated NPC receives knowledge from seed presence.

### Relegation / exit style
`EVT_18_JAN_001` / end-season decisions → `SEED_EXIT_STYLE_UDV` payload → `CEVT_18_RELEG_01` → stay/exit outcome weights differ by clean/contested history. Current contract remains authoritative.

### Injury / physio
`EVT_18_PRE_002`, `EVT_18_MED_001`, age-19 return decisions → `SEED_BODY_PRECEDENT` + `SEED_PHYSIO_CONFIDENCE` → `CEVT_19_INJ_01` → existing two-outcome uncertainty is reweighted; result is not deterministic and no extra RNG stream is introduced.

### Loan return
Early exit/loan decisions → `SEED_EXIT_STYLE_UDV` → `CEVT_19_RETURN_01` → clean loan and contested/pressure histories produce different hierarchy reception probabilities.

### Clara/media
`EVT_18_PRS_001/002` → `SEED_CLARA_CHANNEL` → `CEVT_21_MEDIA_01` → Clara-only choices require the channel; public correction remains public. Knowledge rules target Clara only through explicit channels.

### Adrián comparison
Prior Adrián mirror lineage → `EVT_21_RIV_001` → public choices inform Adrián publicly; `PRIVATE_MESSAGE` informs him directly. Legacy Rivas semantics under the same historical string ID are not reinterpreted.

### Locker cover
`EVT_20_LOCK_002` → `SEED_TEAMMATE_COVER` → future locker escalation can consume it, but this branch does not invent a terminal consumer or a captain identity.

## Partially closed / authority-blocked

### Early market
interest/heat → **must materialize `CareerOffer`** → `EVT_18_JAN_001` / `EVT_18_SUM_001` → response through market authority. Missing producer: age-18 formal alternatives (#123). Direct club mutation from a choice is forbidden.

### Match / press / season end
official fixture + squad + match context → `EVT_18_MATCH_001` / `EVT_18_PRS_001` / `EVT_18_SOC_001` / `EVT_18_END_001`. Shared query surface exists but the canonical producers remain incomplete (#124); null/unavailable fails closed.

### Agent representation
first-agent lineage → active-agent authority → `EVT_20_AGT_001`, `EVT_21_AGT_001`, later renewal/market scenes. Identity resolver exists; representation permission/commission terms are still separate missing facts.

### Market jump / loan
formal offer → role/loan-plan terms → `EVT_20_MKT_001`, `EVT_21_MKT_001`, `CEVT_21_LOAN_01`, `CEVT_22_FREE_01`. `marketHeat` never substitutes for an offer.

### Selection
sporting performance/history → official call-up/list fact → `EVT_21_NAT_001`. National heat/radar is contextual only, not proof of selection.

### External NPC/world events
Rivas role, Vela staff transition, Nano opportunity, Montalbán reappearance, Adrián trajectory, Bruno present need, Dani proposal, UDV crisis and ownership shock require real causal writers before their conditionals can run.

## Identity boundary

T5.5/T5.6 rewrites with the same string IDs are `distinct_scene`, not aliases. Coordinator integration must freeze the current supported generation and add adjacent migration edges. History and pending rows resolve against their saved source content identity + fingerprint.
