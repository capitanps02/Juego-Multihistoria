# Multihistoria · Paquete para PlayCanvas

Destino autorizado: [Historia Jugador, escena 2593315](https://playcanvas.com/editor/scene/2593315).

Archivo: **multihistoria.js**. Incluye motor, UI, estilos y recursos gráficos; no necesita localhost. El nombre registrado del script es **multihistoria**.

**Instalado y verificado el 14 de septiembre de 2026.** El asset ya está subido y Root contiene el componente Script con **multihistoria**. No repetir la instalación ni añadir otra instancia.

[Jugar mediante Launch](https://launch.playcanvas.com/2593315?debug=true). Se verificaron primera elección, resultado tras recargar e historial en Chrome. La escena 3D original se mantiene bajo la interfaz.

No subir fixtures de pruebas ni partidas personales como assets. Para trasladar una partida, usa Tu partida → Descargar copia en el visor local e Importar copia de partida dentro del juego ejecutado en PlayCanvas.

Para actualizar el paquete, ejecuta `npm run build:playcanvas` desde la raíz del proyecto. Ejecuta `npm run test:playcanvas` para verificarlo. `manifest.json` contiene la huella SHA-256 y sus fuentes. No editar manualmente el bundle generado: cambia `web/` o `src/` y vuelve a compilar.

Actualización del 14 de septiembre: adaptador localStorage reforzado durante T2.3, cargado sobre el mismo asset 306862614. Launch recuperó 16 jul 2026 / 4 decisiones sin cambios en la carrera. La versión local del manifiesto identifica este paquete. T2.3 sigue en curso hasta IndexedDB y sus pruebas.

Actualización final T2.3 · 15 de septiembre: IndexedDB instalado y verificado en Launch. SHA-256 `2b25046f75f70d53b2a55264c8a4bba61f0116b9b2d9759b12ca53c432784c24`. La partida online conserva 16 jul 2026 / 4 decisiones tras recargar. La copia localStorage antigua no se borra. Esta entrega sustituye la nota anterior de T2.3 en curso.
