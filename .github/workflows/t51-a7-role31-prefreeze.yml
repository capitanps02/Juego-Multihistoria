name: T51 A7 ROLE31 pre-freeze

on:
  pull_request:
    branches: [main]

jobs:
  role31:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npm run build
      - name: Focused ROLE31 factual regression
        run: node --test scripts/test-a0-a7-role31-prefreeze.mjs
      - name: Emit exact ROLE31 successor identity
        run: |
          node --input-type=module <<'NODE'
          import { EVENTS } from './dist/content/events/index.js';
          import { contentIdentity } from './dist/session/content-identity.js';
          const identity=await contentIdentity(EVENTS);
          const unique=new Set(EVENTS.map(e=>e.id)).size;
          console.log('A7_ROLE31_IDENTITY='+identity);
          console.log('A7_ROLE31_EVENTS='+EVENTS.length);
          console.log('A7_ROLE31_UNIQUE='+unique);
          console.log('A7_ROLE31_ACTIVE='+EVENTS.some(e=>e.id==='EVT_31_ROLE_001'));
          console.log('A7_ROLE31_RETIRED_HOME33='+!EVENTS.some(e=>e.id==='EVT_33_HOME_001'));
          if(EVENTS.length!==388 || unique!==388 || !EVENTS.some(e=>e.id==='EVT_31_ROLE_001') || EVENTS.some(e=>e.id==='EVT_33_HOME_001')) process.exit(1);
          NODE
