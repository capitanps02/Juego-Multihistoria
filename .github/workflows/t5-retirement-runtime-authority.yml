name: T5 Retirement Runtime Authority

on:
  pull_request:
    branches: [main]
  push:
    branches: ['a0/retirement-runtime-authority-61']
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retirement-runtime-authority:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npm run build
      - name: T5 #61 player retirement authority
        run: node --test scripts/test-t5-retirement-authority-known-bug.mjs
      - name: CI-only strict stratified proof
        run: node scripts/qa-t5-sim.mjs
