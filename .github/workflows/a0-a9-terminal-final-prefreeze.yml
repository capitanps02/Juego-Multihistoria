name: A0 A9 terminal final pre-freeze
on:
  pull_request:
    branches: [main]
permissions:
  contents: read
jobs:
  a9-final:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run build
      - name: A9 final terminal regression
        run: node --test scripts/test-a0-a9-terminal-final.mjs
      - name: Emit exact terminal successor identity
        run: |
          node --input-type=module <<'NODE'
          import { EVENTS } from './dist/content/events/index.js';
          import { contentIdentity } from './dist/session/content-identity.js';
          const identity=await contentIdentity(EVENTS);
          console.log('A9_FINAL_IDENTITY='+identity);
          console.log('A9_FINAL_EVENTS='+EVENTS.length);
          console.log('A9_FINAL_UNIQUE='+new Set(EVENTS.map(e=>e.id)).size);
          if(EVENTS.length!==388 || new Set(EVENTS.map(e=>e.id)).size!==388) process.exit(1);
          NODE
