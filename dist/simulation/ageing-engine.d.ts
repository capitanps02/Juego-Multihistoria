import type { GameState } from "../core/types.js";
/** Canon v0.9: age never subtracts a fixed rating. Dimensions drift separately. */
export declare function runMaturityPreseason(state: GameState): void;
export declare function maturityWeek(state: GameState): void;
