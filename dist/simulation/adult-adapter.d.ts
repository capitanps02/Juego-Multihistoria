import type { GameState, State23Tag } from "../core/types.js";
/** Snapshots the state at 23 and opens the adult systems without granting success. */
export declare function adaptState23ToAdult(state: GameState, tags: State23Tag[]): void;
