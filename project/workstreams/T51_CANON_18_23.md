# T5.1 · Workstream canon 18–23

## Estado actual

**Rama:** `t51/canon-18-23`  
**PR:** #10 — audit-only, debe permanecer draft y no se fusiona desde este workstream.  
**Base funcional observada:** `main@6cb81b63f03ce55776ca97075012cfaa22ac228d`.  
**Re-ground limpio:** `86ee71a8d15484c571e840c0cf877feeb97a65c5`, creado con ese `main` como padre directo y preservando únicamente los archivos audit-only propios del workstream.

GitHub, código ejecutable y CI prevalecen sobre resúmenes históricos. Este documento consolida el estado efectivo de la reconciliación 18–23 tras incorporar evidencia ejecutable, contratos compartidos T5.1, el lote B1a, los handoffs B2–B4 y el linaje de contenido vigente. Los lotes 23–26 que `main` integre se heredan para compatibilidad global, pero quedan fuera de la propiedad semántica de este workstream.

Desde el anterior re-ground, `main` ha endurecido además la identidad canónica del origen de seeds (`1d7e51ef821cbcb7b3bbe84ca8cac013ac3f8751`) y ha hecho aditivos los tests de causal facts (`6cb81b63f03ce55776ca97075012cfaa22ac228d`). Estos cambios refuerzan la compatibilidad exigida por B4, pero no alteran los conteos ni las clasificaciones semánticas 18–23.

## Alcance e inventario

Propiedad: inicio, 18–20 y 20–23. 23–26+ queda fuera salvo continuidad mínima. El PR #10 no modifica `EVENTS` activos.

Runtime histórico del scope auditado:

- 18–20: 30 principales + 14 condicionales = 44;
- 20–23: 33 principales + 18 condicionales = 51;
- total: 95.

Canon: 63 principales + 32 condicionales. No hay aliases aprobados.

## Principales — estado efectivo

- **21 `verified_same_identity`**
- **36 `needs_reimplementation`**
- **6 `canonical_missing`**
- **0 `requires_manual_review`**
- **0 `approved_alias`**
- además: **6 `engine_only_noncanonical`**

Las 14 tarjetas 18–20 están cerradas: **5 verified + 9 reimplementation**. Cada tarjeta conserva las 13 dimensiones exigidas de identidad.

### B1a ya integrado y certificado

`main` integra el lote **B1a canonical intel repairs**:

- implementación: `ddfbc08e158a1dc49afccc29ff0680da8f887ccd`;
- merge: `f05667453a1e40d5a4af635e8b9b787bbecdb726`.

Promociones de auditoría:

- `EVT_18_MED_001` → `verified_same_identity`;
- `EVT_18_TEAM_001` → `verified_same_identity`;
- `EVT_18_MATCH_002` → `verified_same_identity`.

Canonical missing principales:

- `EVT_20_BRIDGE_001`
- `EVT_20_MATCH_003`
- `EVT_20_JAN_001`
- `EVT_21_MKT_001`
- `EVT_21_SOC_001`
- `EVT_21_PRS_002`

Engine-only principales:

- `EVT_20_MATCH_001`
- `EVT_21_ABR_001`
- `EVT_21_AGT_002`
- `EVT_21_CCH_001`
- `EVT_22_END_001`
- `EVT_22_LIFE_001`

Los IDs legacy no se renombran ni eliminan silenciosamente: historial/pending y acoplamientos runtime deben conservar verdad histórica durante cada lote funcional.

## Condicionales — estado efectivo

`analysis/T5.1/canon-18-23-conditionals.json` conserva la foto semántica pre-certificación 0/10/15/7. `analysis/T5.1/canon-18-20-conditional-certification.json` es el overlay autoritativo sobre las siete filas manuales: 5 verified + 2 reimplementation + 0 manual.

Estado efectivo de 32 condicionales:

- **5 `verified_same_identity`**
- **12 `needs_reimplementation`**
- **15 `canonical_missing`**
- **0 `requires_manual_review`**
- **0 `approved_alias`**
- además: **15 `engine_only_noncanonical`**

Verificados por certificación ejecutable:

- `CEVT_18_NODEBUT_01`
- `CEVT_18_VELA_01`
- `CEVT_19_BIG_01`
- `CEVT_19_ABROAD_01`
- `CEVT_19_NANO_01`

Exact-ID 20–23 que siguen siendo colisiones semánticas:

- `CEVT_21_ABR_01`
- `CEVT_21_MEDIA_01`
- `CEVT_22_FREE_01`

## Contratos compartidos T5.1

### Session/content migration

Session v3 está integrada con provenance por decisión y lineage multigeneración. Garantías relevantes:

- `sourceContentIdentity` + `eventFingerprint`;
- `resume()` estricto;
- unknown identity rechazada;
- pending legacy conserva definición exacta;
- legacy registry no agenda eventos;
- migración sin RNG;
- lineage acíclica y fail-closed.

Freeze pre-T5.1:

`2e07efd2ea99c4e9ec4c2b20ae89664204f76db2c55a72d567208799c01bccff`

Cada lote futuro que cambie `EVENTS` debe extender el linaje exacto de migración desde todas las fuentes persistidas soportadas y clasificar explícitamente las colisiones semánticas.

### Choice eligibility

Integrado en `76699f93e19e1ac9e17b42ccfaea011d92630fb6`.

Contenido todavía pendiente de cablear:

- `EVT_18_JAN_001`;
- `EVT_18_SUM_001`.

### Event gate alternatives / OR

Integrado en `291e34bfd8bb7c529182a2664564c63ff33eee19`.

Contenido todavía pendiente de cablear:

- `EVT_18_PRS_002`;
- `CEVT_19_SOCIAL_01`;
- `CEVT_21_MEDIA_01`.

**Bloqueos arquitectónicos compartidos conocidos restantes en 18–23: 0.** Eso no significa contenido terminado; significa que el backlog restante es trabajo de escenas/continuidad o depende de hechos del mundo que necesitan writers reales.

## Plan funcional resultante — fuera de PR #10

Batches:

1. **B0 shared contracts:** infraestructura completada.
2. **B1 18–20 repairs:** B1a ya cerró 3 principales; quedan **9 principales + 9 condicionales**.
3. **B2 20–23 rewrites:** **27 principales + 3 condicionales**.
4. **B3 canonical missing:** **6 principales + 15 condicionales**.
5. **B4 legacy retirement:** **6 principales + 15 condicionales**.

### B2 — handoff ejecutable

`analysis/T5.1/canon-20-23-b2-handoff.json` contiene **30 tarjetas explícitas**:

- 27 principales same-ID que todavía pasan por `principal-events.ts -> make(row)` con choices A/B/C/D y `familyEffects` genéricos;
- 3 condicionales same-ID con colisión semántica: `CEVT_21_ABR_01`, `CEVT_21_MEDIA_01`, `CEVT_22_FREE_01`;
- 0 aliases y 0 manuales.

Cada tarjeta fija trigger, opciones canónicas, seeds/memoria, diferencia concreta, riesgo de save y acción, y se asocia a un perfil completo de las 13 dimensiones. Un ID o título idéntico nunca autoriza reinterpretar `history` o un `pending` antiguo como la escena nueva.

Riesgo destacado: `EVT_21_RIV_001` comparte string ID, pero runtime usa Rivas/`HAS_SEED_RIVAS_TRUST` mientras el canon exige Adrián + `SEED_ADRIAN_MIRROR` + contexto mediático. Es una colisión semántica, no una reparación de copy.

### B3 — canonical missing clasificado por dependencia

`analysis/T5.1/canon-20-23-b3-missing-handoff.json` convierte los **6 + 15** ausentes en un contrato de autoría seguro.

Principales:

- `EVT_20_BRIDGE_001` puede autorarse con contratos ya integrados, consumiendo STATE20/contrato/ofertas reales;
- `EVT_20_MATCH_003` queda bloqueado hasta demostrar contexto de partido autoritativo (marcador, orden, stats), sin crear flags cosméticos;
- `EVT_20_JAN_001` necesita interés condicionado real dependiente de una venta de tercero;
- `EVT_21_MKT_001` necesita autoridad de oferta + plan de cesión real;
- `EVT_21_SOC_001` puede autorarse si el contacto Dani/agente tiene provenance explícita;
- `EVT_21_PRS_002` puede autorarse preservando Clara como fuente/medio y sin convertir la seed en oferta garantizada.

Condicionales:

- **9 IDs** están marcados `blocked_external_world_fact`: `CEVT_20_RIVAS_01`, `CEVT_20_VELA_01`, `CEVT_20_NANO_01`, `CEVT_20_MONT_01`, `CEVT_20_ADR_01`, `CEVT_21_BIGCLUB_01`, `CEVT_22_UDV_01`, `CEVT_22_ADR_02`, `CEVT_22_SHOCK_01`;
- los demás requieren comprobar facts compuestos o provenance de oferta/lesión/propuesta antes de autorarse;
- los candidatos legacy son **design lineage**, nunca aliases aprobados.

La regla de B3 es fail-closed: si una transición de staff, lesión de tercero, crisis de club, trayectoria de NPC o cambio de propiedad no tiene un writer causal real, el agente funcional debe detener ese callback concreto y reportar la dependencia. No se permite crear `SOME_FACT=true` únicamente para abrir la escena.

### B4 — retiro legacy compatible cerrado a nivel de auditoría

`analysis/T5.1/canon-18-23-b4-legacy-retirement.json` convierte los **6 principales + 15 condicionales engine-only** en un contrato de retiro por ID.

Disposición común de los 21 IDs:

- `retire_technical_keep_history_only`;
- 0 aliases aprobados;
- no reescritura de `history.eventId`;
- no sustitución directa de `pending` legacy por una escena canónica;
- no fabricación de `SEEN_<canonicalId>` por parecido temático;
- definición legacy congelada disponible para provenance/validación, pero nunca schedulable.

Los 15 condicionales pueden abandonar el catálogo activo cuando exista la generación funcional autorizada y la lineage de migración correspondiente. Sus antiguos candidatos (`CEVT_22_LOANBUY_01`→`CEVT_21_LOAN_01`, `CEVT_21_ADRIAN_01`→callbacks de Adrián, `CEVT_22_RETURN_01`→`CEVT_22_UDV_01`, etc.) son solo **design lineage**, nunca migraciones same-scene.

**Cuatro** principales requieren desacoplamiento explícito antes o atómicamente con su retiro:

1. `EVT_20_MATCH_001` está hardcodeado en `src/narrative/scheduler.ts` como `budgetExempt`. Debe eliminarse o sustituirse esa dependencia y probar el presupuesto de principales de la fase 20 sin el evento técnico.
2. `EVT_21_ABR_001` está todavía en `SEED_FOREIGN_ADAPT.originEvents`. Debe dejar de ser un origen activo futuro, pero seeds históricas con `originEvent=EVT_21_ABR_001` no se reescriben.
3. `EVT_21_AGT_002` está todavía en `SEED_AGENT_POWER.originEvents`. Misma regla: se elimina como origen activo futuro, no se falsea el origen histórico de seeds ya persistidas.
4. `EVT_22_END_001` está hardcodeado en `src/narrative/scheduler.ts` como `budgetExempt`. El batch funcional debe eliminar/reemplazar esa dependencia y probar transición 22→23 y presupuestos antes de retirar el row.

`EVT_21_CCH_001` y `EVT_22_LIFE_001` no presentan un acoplamiento global duro adicional en el barrido dirigido, pero sí aparecen en catálogo/QA y saves históricos; por ello siguen necesitando legacy evidence y migración respaldada.

El endurecimiento T5.2 de `seed.originEvent` integrado en `1d7e51ef…` hace especialmente importante separar dos operaciones distintas: retirar un ID de los `originEvents` activos futuros y preservar intacto el `originEvent` histórico ya persistido en una seed antigua.

La implementación funcional B4 debe demostrar que los 21 IDs ya no se programan, que los saves antiguos siguen siendo veraces, que los cuatro hard couplings desaparecen del runtime activo y que no existe ningún `same_scene` no revisado para esos IDs.

Cada lote que cambie `EVENTS` necesita extensión exacta del linaje de migración, tests dirigidos y QA T5 verde sobre el SHA exacto.

## QA propio

`.github/workflows/t51-canon-18-23.yml` ejecuta build y:

- `scripts/test-t51-canon-18-23.mjs`;
- `scripts/test-t51-repair-plan.mjs`;
- `scripts/test-t51-conditional-certification.mjs`;
- `scripts/test-t51-b2-handoff.mjs`;
- `scripts/test-t51-b3-missing-handoff.mjs`;
- `scripts/test-t51-b4-legacy-retirement.mjs`;
- `scripts/repro-t51-18-20-architecture.mjs`.

Los tests fijan que B1–B4 enumeren IDs exactos. B2 mantiene 27+3 sin aliases/manuales y protege las 13 dimensiones/pending legacy. B3 mantiene 6+15, prohíbe aliases de design lineage y obliga a que los hechos externos críticos permanezcan bloqueados hasta disponer de writers causales reales. B4 fija los 21 retiros, los **cuatro** acoplamientos duros y la invariancia de history/pending/seed-origin.

## Criterio de cierre de esta auditoría

El PR audit-only queda preparado cuando:

- todos los principales y condicionales tienen estado permitido;
- 0 manuales;
- 0 aliases silenciosos;
- contratos compartidos integrados están reflejados;
- B1, B2, B3 y B4 tienen handoff por ID, no solo conteos;
- B4 identifica los cuatro acoplamientos de seed/scheduler antes de cualquier borrado funcional;
- diff aislado a archivos propios;
- PR #10 sigue draft;
- CI del SHA exacto está verde.

T5.1 funcional sigue `in_progress`: quedan 36 principales + 12 condicionales a reimplementar, 6 + 15 escenas canónicas nuevas y los retiros legacy correspondientes. El trabajo audit-only de preparación de B1–B4 queda completo cuando el HEAD actual vuelva a pasar ambos gates.
