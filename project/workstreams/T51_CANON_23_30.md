# T5.1 · Workstream canon 23–30

Rama exclusiva: `t51/canon-23-30`  
Base funcional T5.10: `main@24c176474bb17a8c271f52bac7bf41e08a929c72`  
PR funcional: `#62`  
Fecha: 16/09/2026

## Alcance

Este workstream es responsable de los bloques canónicos **23–26** y **26–30**. El Documento Maestro y la evidencia T5.1 mandan sobre títulos, IDs, gates, opciones, consecuencias, seeds y continuidad. Coincidencia de ID o un antiguo `verified:true` nunca sustituye la comprobación semántica.

Los ficheros protegidos siguen fuera de este workstream:

- `project/PLAN_PASADAS.md`;
- `analysis/2026-09-11/plan-seguimiento.json`.

## Estado global después de T5.10

| Bloque | Principales canon | Principales motor | Condicionales runtime |
|---|---:|---:|---:|
| 23–26 | 40 | 40 | 20 |
| 26–30 | 51 | 51 | 24 |
| **Total** | **91** | **91** | **44** |

Clasificación actual de principales canónicos:

- **28** `verified_same_identity`;
- **37** `needs_reimplementation`;
- **22** `canonical_missing`;
- **4** `requires_manual_review`;
- aliases aprobados: **0**.

En 23–26 quedan **12** principales por reimplementar:

- `EVT_23_CON_001`;
- `EVT_23_EUR_001`;
- `EVT_23_HOME_001`;
- `EVT_23_LOCK_001`;
- `EVT_23_MKT_001`;
- `EVT_23_MONEY_001`;
- `EVT_23_PRS_001`;
- `EVT_24_MATCH_001`;
- `EVT_25_AGT_001`;
- `EVT_25_CON_001`;
- `EVT_25_MKT_001`;
- `EVT_25_NAT_001`.

## Lineage de contenido y compatibilidad de saves

El baseline pre-T5.1 permanece congelado con 388 eventos y `contentIdentity`:

`2e07efd2ea99c4e9ec4c2b20ae89664204f76db2c55a72d567208799c01bccff`

El primer catálogo post-T5.1 integrado por B1a (18–20) es:

`1a8a5e2006fe7160f4fbc02060568d3abec99df038fe3a1a7799c8a0e802eac7`

El catálogo C resultante de **B1a + T5.10** es:

`fee2ff875bac7979d3907f5ee1004ef736efa9a257237c6dee629dd8687fe136`

La ruta válida y única es:

`PRE → B1a → C`

No existe ni debe existir un shortcut `PRE → C`. La infraestructura compartida de lineage multigeneración exige evidencia histórica congelada en cada nodo y falla cerrada ante rutas ausentes, cíclicas o ambiguas.

T5.10 añade únicamente el edge **B1a → C**, con tres mappings `distinct_scene` exact-ID:

- `EVT_23_BRIDGE_001` → `EVT_23_BRIDGE_001`;
- `EVT_23_AGT_001` → `EVT_23_AGT_001`;
- `EVT_23_BODY_001` → `EVT_23_BODY_001`.

Para los tres se libera `SEEN_*` y cooldown canónico. `history` y provenance legacy permanecen intactos y no se reescribe ningún `SeedInstance.originEvent` (`seedOriginMappings = 0`). Las decisiones legacy pendientes continúan dependiendo de su definición/fingerprint congelados hasta resolverse.

El catálogo C queda congelado como evidencia inmutable en:

`qa/fixtures/t5.1/post-t51-sources/fee2ff875bac7979d3907f5ee1004ef736efa9a257237c6dee629dd8687fe136.json`

La registry `src/session/post-t51-legacy-registry.ts` se regeneró mediante los scripts oficiales; no se editó manualmente.

## T5.10 — primer lote funcional 23–26

### `EVT_23_BRIDGE_001` · Ya no eres proyecto

Se sustituye el shell genérico por la transición profesional canónica de entrada en los 23 años.

Guardas:

- one-shot del bloque 23–26;
- no firma ni modifica contrato;
- no crea una oferta formal;
- expresa postura/identidad de carrera antes de un mercado real.

### `EVT_23_AGT_001` · La videollamada sin tu agente

Se implementa la conversación directa con un reclutador como dilema de control y representación.

Guardas:

- exige interés de mercado plausible;
- ninguna opción fabrica una oferta contractual formal;
- conserva el hecho como memoria narrativa sin invadir la autoridad del subsistema de ofertas;
- no concede conocimiento omnisciente a ambos agentes posibles;
- la adquisición epistemológica del agente activo queda fail-closed mientras no exista una resolución dinámica fiable.

La base actual incorpora además el puente integrado de **autoridad narrativa/ofertas/sesión**. Esto refuerza la separación: una escena narrativa puede cambiar memoria y postura, pero las ofertas/contratos siguen bajo su subsistema autorizado.

### `EVT_23_BODY_001` · El GPS

Se reemplaza el shell por la escena canónica de gestión de carga y prevención corporal.

Trigger:

`bodyLoad >= 28` **OR** `HAS_SEED_BODY_PRECEDENT = true`

El OR usa `gateAlternatives`. T5.2 reconoce la rama positiva `HAS_SEED_BODY_PRECEDENT = true` como consumidor diferido real y su política de polaridad evita confundir condiciones negativas/neutras con consumo causal.

La escena escribe `SEED_LOAD_MANAGEMENT` sin fabricar ni reescribir `SEED_BODY_PRECEDENT`.

## QA T5.10

La suite `scripts/test-t51-t510-23-26.mjs` certifica siete propiedades principales:

1. IDs, títulos y opciones canónicas activas;
2. BRIDGE no firma contrato;
3. AGT no fabrica oferta formal;
4. BODY es alcanzable por cualquiera de las dos ramas reales del OR;
5. BODY conserva el origen del antecedente corporal;
6. catálogo activo exacto = C y edge único B1a→C sin PRE→C;
7. PRE→B1a→C preserva history/seeds y libera `SEEN`/cooldown de las tres colisiones exact-ID.

Resultados ya obtenidos antes del último re-ground documental:

- T5.10 dirigido: **7/7 PASS**;
- `check-t51-content-migration-route.mjs`: **PASS**;
- lineage + migración multigeneración: **16/16 PASS**;
- `qa-t5-content.mjs`: **PASS**, `hardErrors: []`;
- catálogo C congelado por el generador oficial;
- registry post-T5.1 regenerada y validada;
- source policy: PRE→B1a→C único, evidencia de endpoints presente y shortcut PRE→C prohibido.

La ejecución completa de `npm test` posterior al congelado C detectó un único test obsoleto: `scripts/test-t51-b1a-intel.mjs` exigía que B1a siguiera siendo el catálogo activo. Eso ya no es cierto una vez existe C. Se corrigió **solo el test compartido**, no contenido 18–20: ahora B1a debe seguir registrado como evidencia histórica inmutable y conservar su ruta PRE→B1a de tres mappings `same_scene`, independientemente de que haya catálogos posteriores.

El HEAD actual se re-groundeó además sobre `main@24c17647…`, que integra el puente de autoridad de ofertas/sesión. La validación definitiva queda en manos de los workflows estándar de PR #62 sobre el HEAD final.

## Condicionales 23–30

La reconciliación de planning de los 44 callbacks está completa, pero **0/44** están certificados a runtime como semánticamente canónicos.

23–26:

- 20/20 IDs exactos;
- 20/20 requieren reescritura semántica específica;
- `CEVT_24_TOURN_02` continúa como contradicción destacada: runtime exige `NATIONAL_CALLED=true` frente a la escena canónica de quedar fuera de la lista final.

26–30:

- 24 callbacks revisados;
- solo 5 IDs exactos;
- 0 migraciones directas same-scene aprobadas;
- todos requieren implementación/rewrite específico.

`CEVT_29_BODY_04` continúa como riesgo alto: mismo ID, pero `bodyLoad` runtime no equivale a la recuperación positiva postcirugía/cuerpo crónico del canon.

## Riesgos principales aún abiertos 26–30

- `EVT_27_MED_001`: canon = “La cirugía puede esperar”/`SEED_SURGERY_TIMING`; runtime = “La final y el isquio”/`FINAL_CONTEXT` + `SEED_BIG_MATCH_BODY`.
- `EVT_26_MKT_001`: el antiguo `verified:true` no supera la evidencia T1 `pilot_repair_required`.
- `EVT_26_BRIDGE_001` ↔ `EVT_26_IDN_001`: candidato, **no alias aprobado**.
- `EVT_27_STAR_001` ↔ `EVT_28_TEAM_001`: no aprobado.
- `EVT_27_AWARD_001` ↔ `EVT_28_GALA_001`: no aprobado.
- `EVT_28_RICH_001` ↔ `EVT_29_MKT_001`: no aprobado.

## T5.2 / seeds y consecuencias diferidas

El owner 23–30 dispone de 59 seeds en el handoff T5.2. La regla sigue siendo **canon first, wiring second**.

T5.10:

- no fabrica el antecedente corporal que solo debe leerse;
- no reescribe orígenes legacy al migrar;
- no usa presencia de seed como sustituto de conocimiento NPC;
- cuenta correctamente la rama positiva `HAS_SEED_BODY_PRECEDENT = true` como consumidor diferido;
- respeta la polaridad de condiciones `HAS_SEED_*`.

## Archivos propios del lote T5.10

Contra la base funcional actual, el lote esperado queda limitado a:

1. `analysis/T5.1/canon-23-30.json`;
2. `project/workstreams/T51_CANON_23_30.md`;
3. `qa/fixtures/t5.1/post-t51-sources/fee2ff875bac7979d3907f5ee1004ef736efa9a257237c6dee629dd8687fe136.json`;
4. `scripts/test-t51-b1a-intel.mjs` — ajuste compartido de regresión multigeneración, sin contenido 18–20;
5. `scripts/test-t51-t510-23-26.mjs`;
6. `src/content/events/23_26/index.ts`;
7. `src/content/events/23_26/t510-principal-events.ts`;
8. `src/session/content-migration.ts`;
9. `src/session/post-t51-legacy-registry.ts` — generado automáticamente.

Los workflows temporales de preparación/probe no forman parte del diff final.

## Siguientes lotes

- **T5.11–T5.14**: resolver los 12 principales restantes de 23–26 en tandas pequeñas, priorizando escenas sin dependencia transversal y separando contrato/oferta, resultados deportivos y nuevas lógicas OR.
- **T5.15–T5.21**: abordar 26–30 tras revalidar ownership y workstreams concurrentes.
- Condicionales: implementar por identidad canónica con política de pending-save explícita; lineage plausible no equivale a alias aprobado.

## Cierre del lote

PR #62 permanece **sin merge**. T5.10 solo se considera cerrado cuando el HEAD final esté a 0 commits de `main`, `T51 content migration` y `Repository integrity` estén verdes, la PR sea mergeable y no haya workflows temporales ni cambios en los dos trackers protegidos. Una vez cumplido, puede pasar de draft a lista para revisión, pero **no se hace merge automático**.
