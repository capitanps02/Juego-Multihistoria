# T5.1 · Workstream canon 23–30

Rama exclusiva: `t51/canon-23-30`  
Base funcional T5.10: `main@46c5f729a8252d5b7ca244ad9e9d9a110098fb09`  
PR funcional: `#62`  
Fecha: 16/09/2026

## Alcance

Este workstream es responsable de los bloques canónicos **23–26** y **26–30**. El Documento Maestro y la evidencia T5.1 mandan sobre títulos, IDs, gates, opciones, consecuencias, seeds y continuidad. Coincidencia de ID o un antiguo `verified:true` nunca sustituye la comprobación semántica.

Los ficheros protegidos siguen fuera de este workstream:

- `project/PLAN_PASADAS.md`;
- `analysis/2026-09-11/plan-seguimiento.json`.

## Estado global después de T5.10

| Bloque | Principales canon | Principales motor | Condicionales runtime |
|---|---:|---:|---:|
| 23–26 | 40 | 40 | 20 |
| 26–30 | 51 | 51 | 24 |
| **Total** | **91** | **91** | **44** |

Clasificación actual de principales canónicos:

- **28** `verified_same_identity`;
- **37** `needs_reimplementation`;
- **22** `canonical_missing`;
- **4** `requires_manual_review`;
- aliases aprobados: **0**.

En 23–26 quedan **12** principales por reimplementar:

- `EVT_23_CON_001`;
- `EVT_23_EUR_001`;
- `EVT_23_HOME_001`;
- `EVT_23_LOCK_001`;
- `EVT_23_MKT_001`;
- `EVT_23_MONEY_001`;
- `EVT_23_PRS_001`;
- `EVT_24_MATCH_001`;
- `EVT_25_AGT_001`;
- `EVT_25_CON_001`;
- `EVT_25_MKT_001`;
- `EVT_25_NAT_001`.

## Freeze y compatibilidad de contenido

El baseline pre-T5.1 está congelado en:

- 388 eventos = 254 principales + 134 condicionales;
- `contentIdentity`: `2e07efd2ea99c4e9ec4c2b20ae89664204f76db2c55a72d567208799c01bccff`.

Session v3 y la política integrada de migración permiten modificar contenido activo cuando el drift tiene una ruta explícita. Mantener el mismo event ID no hace que una escena reescrita sea automáticamente la misma escena para una partida guardada.

T5.10 registra una ruta explícita hacia:

`5f6a871e4124498aab29903327739037a52b50e6505026bd95824d275d92eaae`

La ruta contiene tres mappings `distinct_scene` exact-ID:

- `EVT_23_BRIDGE_001` → `EVT_23_BRIDGE_001`;
- `EVT_23_AGT_001` → `EVT_23_AGT_001`;
- `EVT_23_BODY_001` → `EVT_23_BODY_001`.

Para los tres se libera `SEEN_*` y cooldown canónico después de migrar. El history ya resuelto y su provenance legacy permanecen intactos. No se reescribe ningún `SeedInstance.originEvent` (`seedOriginMappings = 0`). Una escena legacy ya pendiente conserva su definición congelada hasta resolverse.

## T5.10 — primer lote funcional 23–26

### `EVT_23_BRIDGE_001` · Ya no eres proyecto

Se sustituye el shell genérico por la transición profesional canónica de entrada en los 23 años.

Guardas de implementación:

- one-shot del bloque 23–26;
- no firma contratos;
- no altera una oferta formal;
- las opciones expresan postura/identidad de carrera y no simulan un mercado inexistente.

### `EVT_23_AGT_001` · La videollamada sin tu agente

Se implementa la conversación directa con un reclutador como dilema de control y representación.

Guardas de implementación:

- exige una señal de interés de mercado plausible;
- ninguna opción crea por sí sola una oferta contractual formal;
- la seed/memoria narrativa conserva el hecho sin saltarse la autoridad del subsistema de ofertas;
- no se concede conocimiento omnisciente a ambos agentes posibles. Mientras no exista resolución dinámica fiable del agente activo, la adquisición epistemológica queda **fail-closed** para T5.3.

### `EVT_23_BODY_001` · El GPS

Se reemplaza el shell por la escena de gestión de carga y prevención corporal.

Trigger causal real:

`bodyLoad >= 28` **OR** `HAS_SEED_BODY_PRECEDENT = true`.

El OR usa el contrato compartido `gateAlternatives`; no se aproxima con AND ni se materializa una seed previa inexistente. La resolución escribe memoria de gestión de carga sin falsificar el origen del antecedente corporal.

La base final `main@46c5f729…` incorpora además la auditoría T5.2 que reconoce `HAS_SEED_*` dentro de `gateAlternatives` como consumidor diferido real. Esto aumenta la cobertura transversal de BODY sin modificar su semántica.

## Tests y validación T5.10

Suite dirigida: `scripts/test-t51-t510-23-26.mjs`.

Preflight funcional ya validado:

- **7/7 PASS**;
- IDs/títulos/opciones canónicas activos;
- BRIDGE no firma contrato;
- AGT no crea oferta formal por efectos;
- BODY demuestra reachability por cualquiera de las dos ramas del OR;
- BODY no reescribe `SEED_BODY_PRECEDENT`;
- target `contentIdentity` exacta validada;
- migración preserva history y libera `SEEN`/cooldown legacy para las escenas nuevas.

También pasaron antes del último re-ground:

- `npm run build`;
- `node scripts/check-t51-content-migration-route.mjs`;
- `node scripts/qa-t5-content.mjs` con `hardErrors: []`;
- `npm test` completo.

Durante la regresión se detectó y corrigió un bug del propio harness de migración: el fixture sintético declaraba `gateAlternatives = []`. El contrato OR falla cerrado ante alternativas explícitas vacías, por lo que el evento de prueba era inalcanzable. El fixture ahora omite esa propiedad y usa `hard_deadline` únicamente para eliminar ruido de ritmo/presupuesto. No se cambió runtime para hacer pasar el test.

El workstream se re-groundeó primero sobre el contrato T5.3 de contactos del protagonista y después sobre `main@46c5f729…`, que añade la auditoría T5.2 de consumidores en OR gates. Ninguno de esos cambios pisa los siete archivos propios de T5.10. La validación definitiva es la CI de PR #62 sobre el HEAD final re-groundeado.

## Condicionales 23–30

La reconciliación de planning de los 44 callbacks está completa, pero **0/44** están certificados a runtime como semánticamente canónicos.

23–26:

- 20/20 IDs exactos;
- 20/20 requieren reescritura semántica específica;
- contradicción destacada: `CEVT_24_TOURN_02` usa `NATIONAL_CALLED=true` en runtime frente a la escena canónica de quedar fuera de la lista final.

26–30:

- 24 callbacks revisados;
- solo 5 IDs exactos;
- 0 migraciones directas same-scene aprobadas;
- todos requieren implementación/rewrite específico.

`CEVT_29_BODY_04` continúa como riesgo alto: mismo ID, pero la carga corporal runtime no equivale a la recuperación positiva postcirugía/cuerpo crónico del canon.

## Riesgos principales aún abiertos 26–30

- `EVT_27_MED_001`: mismo ID, canon = “La cirugía puede esperar”/`SEED_SURGERY_TIMING`; runtime = “La final y el isquio”/`FINAL_CONTEXT` + `SEED_BIG_MATCH_BODY`.
- `EVT_26_MKT_001`: el histórico `verified:true` no supera la evidencia T1 `pilot_repair_required`.
- `EVT_26_BRIDGE_001` ↔ `EVT_26_IDN_001`: candidato de reconciliación, **no alias aprobado**.
- `EVT_27_STAR_001` ↔ `EVT_28_TEAM_001`: no aprobado.
- `EVT_27_AWARD_001` ↔ `EVT_28_GALA_001`: no aprobado.
- `EVT_28_RICH_001` ↔ `EVT_29_MKT_001`: no aprobado.

## T5.2 / seeds y consecuencias diferidas

El owner 23–30 dispone de 59 seeds en el handoff T5.2. La regla se mantiene: **canon first, wiring second**. No se añaden consumidores o terminales artificiales a shells genéricas solo para mejorar métricas.

T5.10 respeta esa regla:

- no fabrica el antecedente corporal que solo debe leerse;
- no reescribe orígenes legacy durante la migración;
- no usa presencia de seed como sustituto de conocimiento NPC;
- el auditor T5.2 actual ya cuenta correctamente el consumo de `SEED_BODY_PRECEDENT` dentro de `gateAlternatives`.

## Archivos del lote T5.10

El diff propio esperado contra `main@46c5f729…` queda limitado a:

1. `analysis/T5.1/canon-23-30.json`;
2. `project/workstreams/T51_CANON_23_30.md`;
3. `scripts/test-t51-content-migration.mjs`;
4. `scripts/test-t51-t510-23-26.mjs`;
5. `src/content/events/23_26/index.ts`;
6. `src/content/events/23_26/t510-principal-events.ts`;
7. `src/session/content-migration.ts`.

El workflow temporal `.github/workflows/t510-content-probe.yml` fue eliminado y no forma parte del diff final.

## Siguientes lotes

- **T5.11–T5.14**: resolver los 12 principales restantes de 23–26 en tandas pequeñas, empezando por escenas sin dependencia transversal y separando las que requieren contrato/oferta, resultado deportivo o lógica OR adicional.
- **T5.15–T5.21**: 26–30 solo después de revalidar el estado de los workstreams/PR concurrentes y su ownership efectivo.
- Condicionales: implementar por identidad canónica y con política de pending-save explícita; no convertir lineage plausible en alias implícito.

## Cierre del lote

PR #62 está abierta y **no debe mergearse automáticamente**. T5.10 queda listo para cierre cuando `T51 content migration` y `Repository integrity` estén verdes sobre el HEAD final de `t51/canon-23-30`, el branch esté a 0 commits por detrás de `main` vigente y la PR permanezca mergeable.
