# T5.1 23–26 — micro-generación LOCK

Base de trabajo: `main@6cb81b63f03ce55776ca97075012cfaa22ac228d`  
Rama: `integration/t511-lock-23-26`  
Fecha: 16/09/2026

## Alcance

Esta micro-generación reimplementa únicamente `EVT_23_LOCK_001` — **Las cuatro de la mañana** — a partir de la ficha canónica B665.

La generación activa anterior es E, introducida por `EVT_23_PRS_001`:

`88751a2107c035826162968991e3a1808b2f4573afa3a3a20a3efa376fa8af1f`.

LOCK deberá producir una única generación sucesora F y una sola arista E → F. No se permiten shortcuts desde PRE/B1a/C/D.

## Canon

Trigger canónico: afinidad con `SLOT_CAPTAIN` o `SLOT_STAR`, **OR** precedente `SEED_TEAMMATE_COVER`.

El runtime compartido ya resuelve de forma autoritativa:

- UDV / fase 23–26 / `captain = NPC_PLR_10`;
- `star = null` mientras no exista evidencia canónica suficiente;
- `facts.lockerCaptainAffinity` y `facts.lockerStarAffinity` como facts derivados read-only.

Se adopta `affinity >= 60` como **umbral técnico de reachability**, no como cifra del Documento Maestro. Es el mismo umbral usado por la regresión propietaria del contrato compartido de liderazgo.

Las cuatro decisiones canónicas son:

1. cubrir al compañero;
2. negarse sin avisar a nadie;
3. pedirle que admita el retraso y acompañarlo;
4. avisar al capitán antes de que el asunto llegue al técnico.

## Verdad, NPC y conocimiento

La identidad del compañero que pide la coartada no está fijada por la ficha canónica ni por un contrato runtime autoritativo. Por tanto esta pasada **no inventa un NPC**, no añade `npcRefs` estáticos y no convierte afinidad con cualquier compañero en sustituto de un slot.

La opción D sí informa al capitán resuelto por `targetSlots: ["captain"]`. La regla está acotada por choice y por ambos outcomes. Las opciones A/B/C no hacen omnisciente al capitán.

La escena explicita que la coartada es falsa y que no existe delito ni una situación de seguridad en curso. No fabrica una investigación, una sanción ni pruebas que el estado no posea.

## Seeds y ownership

`SEED_TEAMMATE_COVER` se usa solamente como antecedente de eligibility. La seed pertenece al precedente `EVT_20_LOCK_002`, tiene alcance `origin_club` y no se relabela ni se recrea en LOCK23.

Esta micro-generación:

- lee `SEED_TEAMMATE_COVER`;
- no escribe seeds;
- no modifica `originEvents`;
- no requiere `seedOriginMappings`.

## Compatibilidad esperada

`EVT_23_LOCK_001` es una colisión semántica exact-ID (`needs_reimplementation`), por lo que la futura arista E → F deberá usar `distinct_scene` y liberar únicamente:

- `SEEN_EVT_23_LOCK_001`;
- cooldown de `EVT_23_LOCK_001`.

History, decision provenance, seeds, RNG y pending legacy deben conservarse. La fixture E se usará para una migración real E → F sin drift RNG.

## QA dirigido

`scripts/test-t51-lock-23-26.mjs` cubre:

- identidad/título/decisiones canónicas;
- OR exacto por captain/star/seed con umbral técnico 60;
- no aceptación de afinidad de un NPC no líder;
- invalidación del captain al cambiar de club;
- ausencia de contrato, mercado, cambio de club o seed fabricada;
- regla dinámica exclusiva para la opción D;
- conocimiento del captain solo cuando se le informa;
- ausencia de omnisciencia en las otras decisiones.

Tras generar F se añadirán pruebas de identity, lineage E → F, liberación selectiva del scheduler y migración desde la fixture E.

## Estado del lote

Antes de LOCK el bloque 23–26 acumulaba 7 escenas canónicas nuevas/reimplementadas del primer conjunto de 12. LOCK elevará el total a **8/12** cuando la generación F quede congelada, migrable y con CI exact-head verde.

Pendientes principales de ese conjunto: `EVT_23_MKT_001`, `EVT_23_CON_001`, `EVT_24_MATCH_001` y `EVT_25_CON_001`, todos sujetos a sus contratos de autoridad compartida correspondientes.

No se realiza merge desde este workstream.
