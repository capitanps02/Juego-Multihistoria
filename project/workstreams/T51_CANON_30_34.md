# T5.1 — Reconciliación canónica 30–34

## Alcance y reglas

Rama exclusiva: `t51/canon-30-34`.

Este workstream es propietario del bloque canónico 30–34 del Documento Maestro. No modifica arquitectura global de scheduler, aging, retirement, save schema, NPC core o UI. Tampoco modifica `project/PLAN_PASADAS.md` ni `analysis/2026-09-11/plan-seguimiento.json`.

El Documento Maestro prevalece. Ninguna coincidencia por título, tema o parecido visual se considera alias. Para aprobar identidad deben coincidir trigger, opciones, conocimiento visible, incertidumbre, consecuencias, seeds, conocimiento NPC y continuidad de save.

## Estado estricto del inventario

- 50 principales canónicos.
- 50 principales en motor.
- 45 `needs_reimplementation`.
- 5 `canonical_missing`.
- 5 `engine_only_noncanonical`.
- 0 `approved_alias`.
- 0 `verified_same_identity`.
- 26 condicionales del motor siguen `requires_manual_review` por ausencia de inventario condicional canónico autoritativo.

La igualdad 50/50 no implica igualdad de identidades. Existen IDs desplazados, identidades faltantes y escenas engine-only.

## Safe stable-ID pass completado

El conjunto estable seguro está agotado: **27 escenas** con ID canónico ya existente reciben semántica de decisión del Documento Maestro sin renombrados destructivos ni cambios de arquitectura compartida.

Distribución:

- edad 30: 8;
- edad 31: 10;
- edad 32: 5;
- edad 33: 4.

Todas permanecen exportadas como `canonStatus: technical_adaptation` y tag `t51_canonical_reimplementation`. Ninguna se promueve a `verified_same_identity`: todavía hay trigger/state/downstream parity aproximada o no demostrada.

## Matriz ejecutable 27 / 18 / 5

`analysis/T5.1/canon-30-34-implementation-readiness.json` particiona exactamente los 50 principales:

- 27 `stable_semantics_implemented_shared_parity_gap`;
- 18 `shifted_identity_requires_migration`;
- 5 `canonical_missing_requires_coordinated_addition`.

`scripts/test-t51-30-34-readiness.mjs` deriva los 18 shifted y los 5 missing desde el audit estricto y bloquea drift silencioso.

Además protege el estado de OR gates: el contrato `gateAlternatives` ya existe en `main`, por lo que `DEP_T51_OR_GATES` está cerrado. La deuda real de `EVT_30_CON_001` es `DEP_T51_RENEWAL_INTENT_FACT`: falta una señal causal autoritativa de “el club quiere renovar”.

## Handoff de migración para el batch actual

`analysis/T5.1/canon-30-34-migration-handoff.json` especifica la ruta que debe registrar el propietario de integración sin modificar `CONTENT_MIGRATION_ROUTES` desde este workstream.

Para el batch actual:

- source content identity congelada post-B1a: `1a8a5e2006fe7160f4fbc02060568d3abec99df038fe3a1a7799c8a0e802eac7`;
- target runtime real actual: `163b582315bb8d08ac4a03f13719b89c0d76c130a2b33f3e32fa53af5474d8dc`;
- 27 mappings exact-ID `distinct_scene`;
- 0 mappings `same_scene`;
- 0 reescrituras de origen de seeds;
- los 27 mappings limpian `SEEN` y cooldown canónico para que la historia legacy con mismo string no suprima la escena reparada.

El checker compartido exige además un **target freeze `163b5823…`** en `qa/fixtures/t5.1/post-t51-sources/` antes del registro del edge. Ese freeze y el registro pertenecen a integración, no a este workstream.

No se anticipan mappings para futuros batches:

- 18 identidades desplazadas: sin mapping todavía, `sameSceneMigrationAllowed:false`;
- 5 missing: sin historia/SEEN/cooldown sintetizados;
- 5 engine-only: siguen activos en este batch; `ENGINE_ONLY_HISTORY` solo permite retirarlos de forma segura cuando un batch futuro lo autorice explícitamente.

Caso especial documentado: `EVT_31_TEAM_001` tiene doble colisión semántica. El principal canónico “Mentor oficial” se aproxima al legacy `EVT_31_MENT_001`, mientras que el string legacy `EVT_31_TEAM_001` representa otra identidad relacionada con el canónico `EVT_31_SQUAD_001`. No hay alias aprobado en ninguna dirección.

## EVT_30_CON_001

Trigger canónico: `contract <= 18 months OR club wants renewal`.

`main` ya soporta OR gates mediante `gateAlternatives`; por tanto OR ya no es bloqueo arquitectónico. GameState aún no produce un hecho autoritativo de intención de renovación. La escena conserva `t51_unmodelled_club_renewal_proxy` y `t51_trigger_approximation`, pero no se declara verificada.

## Seeds y consecuencias diferidas

`analysis/T5.1/canon-30-34-seed-lifecycle.json` demuestra siete cadenas reales sin inventar consumidores:

- `SEED_AGE30_CONTRACT`;
- `SEED_MATCH_SELECTIVITY`;
- `SEED_NATIONAL_PHASEDOWN`;
- `SEED_CAPTAIN_HANDOVER`;
- `SEED_AGENT_LAST_CONTRACT`;
- `SEED_SURGERY_31`;
- `SEED_HOME_RETURN_31`.

Open seeds permanecen abiertas mientras no exista consumidor o transición terminal demostrable en canon o en un bloque downstream propietario.

## Missing canónicos

1. `EVT_30_CCH_001` — Te enteras por la pizarra.
2. `EVT_30_PRS_001` — El ultimátum que nunca diste.
3. `EVT_30_NAT_002` — La selección gana sin ti.
4. `EVT_30_JAN_001` — Enero: especialista de lujo.
5. `EVT_31_ROLE_001` — Tres goles y al banquillo.

No se añaden todavía porque alterarían el inventario activo y el `contentIdentity`; deben entrar en un batch coordinado con ruta de migración correspondiente.

## Identidades desplazadas

Los 18 desplazamientos siguen fuera de aprobación automática. Entre los candidatos principales están:

- `EVT_30_BRIDGE_001` ↔ `EVT_30_IDN_001`;
- `EVT_30_STATUS_001` ↔ `EVT_30_TEAM_001`;
- `EVT_30_EUR_001` ↔ `EVT_30_FINAL_001`;
- `EVT_30_RECORD_001` ↔ `EVT_31_REC_001`;
- `EVT_30_PAIN_001` ↔ `EVT_33_MED_001`;
- `EVT_30_NANO_001` ↔ `EVT_33_NET_001`;
- `EVT_31_FAM_001` ↔ `EVT_30_LIFE_001`;
- `EVT_31_TEAM_001` ↔ `EVT_31_MENT_001`;
- `EVT_31_SQUAD_001` ↔ legacy `EVT_31_TEAM_001`;
- `EVT_31_BIZ_001` ↔ `EVT_32_IMG_001`;
- `EVT_32_RICH_001` ↔ `EVT_32_MKT_001`;
- `EVT_32_ELITE_001` ↔ `EVT_32_CLB_001`;
- `EVT_32_IMPACT_001` ↔ `EVT_32_TACT_001`;
- `EVT_32_SUCCESSOR_001` ↔ `EVT_32_TEAM_001`;
- `EVT_32_LOAD_001` ↔ `EVT_32_BODY_001`;
- `EVT_32_BOSMAN_001` ↔ `EVT_32_BOS_001`;
- `EVT_33_RECORD_001` ↔ `EVT_33_REC_001`;
- `EVT_33_FIN_001` ↔ `EVT_33_END_001`.

No se aprueba ningún alias por parecido.

## Engine-only

- `EVT_30_AGT_001`;
- `EVT_33_NAT_001`;
- `EVT_33_HOME_001`;
- `EVT_33_CON_001`;
- `EVT_33_RET_001`.

`ENGINE_ONLY_HISTORY` ya está certificado en Session v3, pero decidir cuáles se retiran del catálogo activo sigue siendo decisión canónica coordinada. Este PR no los elimina silenciosamente.

## Retirada 30–34

Invariante protegida:

- la edad cronológica no fuerza retirada;
- `classifyState34` solo es terminal si `flags.EARLY_RETIRED_30_34 === true`;
- `STATE34_RETIREMENT_NEAR` es no terminal;
- `EVT_33_RET_001` es voluntario y condicionado;
- retirada internacional (`NATIONAL_RETIRED`) no equivale a retirada de club.

## Dependencias abiertas

- `DEP_T51_EVENT_ID_MIGRATION`: el lineage global existe; integración debe congelar primero `163b5823…` como evidencia post-T5.1 y registrar/certificar después el edge único `T51_B1A → 163b5823…` con los 27 `distinct_scene`.
- `DEP_T51_BRIDGE_PRIORITY`: `EVT_30_BRIDGE_001` debe ser puente obligatorio de inicio de Pasada 6; el scheduler ordinario no lo garantiza.
- `DEP_T51_RENEWAL_INTENT_FACT`: falta hecho causal producido para intención de renovación.
- `DEP_T51_CONDITIONAL_CANON`: faltan IDs/identidades condicionales canónicas autoritativas.
- `DEP_T51_SEED_TOPOLOGY`: no cerrar seeds sin evidencia downstream.
- `DEP_T51_B1A_TEST_LINEAGE_STALENESS`: el test compartido B1a debe pasar de “B1a es el runtime final” a validar el edge `PRE_T51 → B1A` dentro de un lineage multi-batch.

## Validación vigente

Baseline sincronizado: `main@24c176474bb17a8c271f52bac7bf41e08a929c72`.

Evidencia dirigida:

- run `35127976484`: **29/29 PASS** focalizado; **offer/session bridge PASS**; checker de migración reproduce únicamente la ausencia esperada del freeze `163b5823…` antes del registro de ruta.

- run `35127605089`: **29/29 PASS** en la suite focalizada 30–34 tras rebasar el handoff a `T51_B1A → 163b5823…`; el test compartido B1a reproduce por separado su única aserción lineage-stale.

- run `35119172121`: **29/29 PASS**, incluyendo handoff de migración contra `contentIdentity(EVENTS)` real;
- run `35123290229`: **29/29 PASS** después de añadir el guard permanente que rechaza `DEP_T51_OR_GATES`, `pending_shared` o wording obsoleto;
- run `35123425745`: **PASS** tras sincronizar evidencia del audit; vuelve a ejecutar `npm run test:t51` antes del commit final y autoelimina su workflow temporal.

Shared contract:

- run `35123405987`: choice eligibility/shared build **PASS** sobre el HEAD permanente previo a la sincronización final de evidencia.

La infraestructura global ya preserva el lineage `PRE_T51 → T51_B1A`. El boundary esperado del batch 30–34 es ahora el edge `T51_B1A → 163b5823…`, todavía pendiente de registro/certificación por integración. El test compartido `test-t51-b1a-intel.mjs` conserva además una aserción obsoleta que trata B1a como identidad activa final; se registra como dependencia global y no se reescribe desde este workstream.

Estado del audit:

- `sharedOrGateContract: available_in_main`;
- `contentMigrationLineage: PRE_T51_to_T51_B1A_available_in_main`;
- `contentMigrationRoute: handoff_B1A_to_163b_registration_pending`;
- `targetedT51Result: 29/29_pass`;
- sin `DEP_T51_OR_GATES` ni `pending_shared` residuales.

## Diff permanente

16 archivos permanentes. No quedan workflows temporales y no se modifican los archivos protegidos/global-ownership prohibidos.

## Estado de cierre

La pasada segura 30–34 está completada, pero el bloque **no está canónicamente cerrado ni merge-ready**.

Pendientes principales:

- registro/certificación global de la ruta de migración del batch actual;
- 18 identidades desplazadas para batch futuro;
- 5 principales canónicos missing;
- paridad estricta de los 27 estables;
- inventario condicional autoritativo;
- prioridad obligatoria del bridge;
- hecho causal de intención de renovación;
- consumidores/terminales downstream de seeds abiertos cuando el canon lo demuestre.

PR #13 debe permanecer **draft**. **DO NOT MERGE**.