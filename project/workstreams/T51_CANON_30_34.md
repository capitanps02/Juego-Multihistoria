# T5.1 — Canon 30–34

## Ownership

Rama exclusiva: `t51/canon-30-34`.

Este workstream reconcilia e implementa de forma segura el bloque canónico 30–34. El Documento Maestro prevalece. No modifica `main` directamente, no hace merge automático, no registra rutas globales de migración y no crea el freeze global post-T5.1.

## Baseline vigente

Último re-ground confirmado:

`main@6d2239ae1f89be97a7c5cf117d456cff9218aade`

Inventario estricto:

- 50 principales canónicos;
- 27 IDs estables con semántica canónica reimplementada como `technical_adaptation`;
- 18 identidades desplazadas que requieren migración explícita;
- 5 principales canónicos missing;
- 5 engine-only activos;
- 0 aliases aprobados;
- 0 `verified_same_identity`;
- 26 condicionales del motor todavía sin inventario condicional canónico autoritativo.

La igualdad 50/50 entre principal canon y principal engine no implica igualdad de identidad.

## Autoridad compartida ya integrada

### Renovación

`EVT_30_CON_001` usa el trigger canónico exacto:

`contract.monthsRemaining <= 18 OR facts.clubWantsRenewal == true`

mediante `gateAlternatives`.

### Carrera / contrato

El catálogo activo 30–34 aplica un guard que elimina escrituras narrativas sobre estado propiedad de `CareerOffer/respondToOffer`, incluyendo club/owner/registration/tier/route/prestige, `contract.*` y flags de transferencia/préstamo/gran club.

Tres escenas consumen ofertas formales:

- `EVT_31_HOME_001` — oferta UDV;
- `EVT_32_HOME_001` — oferta UDV;
- `EVT_32_CON_001` — renovación formal del mismo club, 12 meses, reason `Renovación de contrato`.

`EVT_32_CON_001` sigue sin paridad contractual completa porque `CareerTerms` no persiste la cláusula canónica de renovación automática por minutos.

### Mercado

`MarketState.pending` representa cero-o-una `CareerOffer`. Por tanto:

- `EVT_31_MKT_001` no recibe `offerBridge`: necesita dos ofertas simultáneas;
- `EVT_33_MKT_001` no recibe `offerBridge`: necesita cuatro propuestas/rutas simultáneas.

No se fabrica pluralidad desde `marketHeat`.

### Deporte

Los hechos concretos solo pueden venir de `facts.sport` / `facts.match`. Mientras competición, fixtures, convocatoria, banquillo, titularidad, minutos, resultado y producción deportiva sigan `null`, no se sustituyen por forma/edad/rol/confianza/mes/flags.

Fail-closed ya aplicado:

- `EVT_31_FINAL_001` requiere competición + próximo fixture;
- `EVT_33_BODY_001` requiere próximo fixture + horas hasta fixture.

Deuda scene-by-scene: `analysis/T5.1/canon-30-34-authority-debt.json`.

## Seeds / memoria causal

Audit compartido owner `t51/canon-30-34`:

- 52 seeds asignadas;
- 48 con productor runtime;
- 4 sin productor;
- 13 con algún consumidor estructural;
- 39 sin consumidor;
- 0 terminales explícitos;
- 52 open-ended;
- 7 `declaredReadMismatches`.

`withAnyConsumer` no significa consumidor canónico certificado.

### Cuatro seeds huérfanas

Writer canónico resuelto uno-a-uno:

- `SEED_ROLE_COMMUNICATION` → `EVT_30_CCH_001`;
- `SEED_FALSE_ULTIMATUM` → `EVT_30_PRS_001`;
- `SEED_NATIONAL_ABSENCE` → `EVT_30_NAT_002`;
- `SEED_SPECIALIST_BIGCLUB` → `EVT_30_JAN_001`.

No pueden conectarse a eventos legacy/genéricos. `PASADA_6_30_34` es provenance editorial, no `originEvent` runtime.

Guard: `scripts/test-t51-30-34-seed-handoff.mjs`.

### Handoff frontera 34+ / retirada

`analysis/T5.1/canon-30-34-downstream-seed-handoff.json` documenta sin imponer consumidores:

- `SEED_AGE34_PRIORITY` — writers actuales `EVT_33_CON_001`, `EVT_33_END_001`, `EVT_33_MKT_001`;
- `SEED_RETIREMENT_PUBLIC_TONE` — `EVT_33_PRS_001`;
- `SEED_HOME_PULL_PUBLIC` — `EVT_33_HOME_001`, `EVT_33_PRS_001`;
- `SEED_FINAL_FOUR_WAYS` — `EVT_33_MKT_001`;
- `SEED_VETERAN_LEADERSHIP_FINAL` — `EVT_33_CAP_001`;
- `SEED_72H_LIMIT` — `EVT_33_BODY_001`.

Los orígenes múltiples se preservan; no se colapsan a provenance falsa.

### `seedsRead` no equivale a consumo causal

Siete enlaces principal→principal antes sobreclasificados como `confirmedChains` quedaron degradados a `declaredReadLinks` porque solo declaran memoria contextual y no tienen dependencia runtime de la seed:

- `SEED_AGE30_CONTRACT` → `EVT_31_MKT_001`, `EVT_32_CON_001`;
- `SEED_MATCH_SELECTIVITY` → `EVT_33_BODY_001`;
- `SEED_NATIONAL_PHASEDOWN` → `EVT_32_NAT_001`;
- `SEED_CAPTAIN_HANDOVER` → `EVT_33_CAP_001`;
- `SEED_AGENT_LAST_CONTRACT` → `EVT_31_AGT_001`;
- `SEED_SURGERY_31` → `EVT_31_RETURN_001`;
- `SEED_HOME_RETURN_31` → `EVT_32_HOME_001`.

`causalChainsConfirmed = []` para ese subconjunto.

Control positivo: `SEED_CHRONIC_BODY → EVT_31_MED_001` sí es consumo runtime real porque usa `HAS_SEED_CHRONIC_BODY`.

Guard: `scripts/test-t51-30-34-seed-consumption.mjs`.

### 7 `declaredReadMismatches` condicionales

`analysis/T5.1/canon-30-34-conditional-seed-read-debt.json` demuestra que los siete mismatches owner 30–34 sí son consumidores runtime positivos mediante `HAS_SEED_*`:

- `CEVT_31_SURGERY_01` → `SEED_SURGERY_31`;
- `CEVT_31_COACH_01` → `SEED_NEW_COACH_RESET`;
- `CEVT_31_NTLOAD_01` → `SEED_CLUB_NT_LOAD_TENSION`;
- `CEVT_31_FINAL_01` → `SEED_MANAGED_FINAL_ROLE`;
- `CEVT_32_REPLACE_01` → `SEED_REPLACEMENT_BREAKOUT`;
- `CEVT_32_BOSMAN_01` → `SEED_BOSMAN_33`;
- `CEVT_32_FAN_01` → `SEED_FAN_LEGACY_BUFFER`.

La deuda es de metadata (`seedsRead` ausente), no de reachability runtime. Sin embargo, esos condicionales siguen `technical_adaptation` y carecen de identidad canónica autoritativa, por lo que consumo runtime != consumo canónico certificado.

Añadir `seedsRead` cambia `contentIdentity` porque el catálogo se hashea con `JSON.stringify(EVENTS)`. La corrección metadata se difiere hasta que integración congele/registre el target actual y exista decisión de identidad condicional; no se elimina ningún gate funcional para silenciar el audit.

Guard: `scripts/test-t51-30-34-conditional-seed-reads.mjs`.

## Missing principales

Batch coordinado pendiente:

1. `EVT_30_CCH_001` — writer de `SEED_ROLE_COMMUNICATION`;
2. `EVT_30_PRS_001` — writer de `SEED_FALSE_ULTIMATUM`;
3. `EVT_30_NAT_002` — writer de `SEED_NATIONAL_ABSENCE`;
4. `EVT_30_JAN_001` — writer de `SEED_SPECIALIST_BIGCLUB`;
5. `EVT_31_ROLE_001` — además bloqueado por goles recientes + banquillo real.

Su alta cambia `contentIdentity`; debe entrar después del freeze/edge actual y con una nueva ruta/freeze coordinados.

## Identidades desplazadas

Siguen pendientes las 18 parejas documentadas en `analysis/T5.1/canon-30-34-migration-handoff.json`. Todas mantienen `sameSceneMigrationAllowed:false`. No hay aliases silenciosos ni renombrados destructivos.

Caso especialmente sensible: `EVT_31_TEAM_001` participa en una colisión semántica doble y no puede resolverse por string matching.

## Bridge 30→34

La infraestructura compartida de transition priority existe, pero `EVT_30_BRIDGE_001` continúa desplazado frente a `EVT_30_IDN_001`. No se hace obligatorio el legacy antes de resolver identidad + migración.

## Retirada

Invariantes:

- edad no fuerza retirada;
- retirada internacional != retirada de club;
- memoria de retirada no es estado terminal;
- `EVT_33_RET_001` sigue siendo una adaptación técnica de retirada temprana voluntaria, no auto-retirada por edad.

## Migración vigente

Source congelada usada por handoff:

`5d3fd71a8df42ed5b93fdde63ed386e9d776693addd62a30293dbecad7aa56d2`

Target actual del catálogo:

`14164e54ec4250e50b915c29c959b35a56ff7ccd249a99c4ad026925af2d8c07`

Freeze requerido:

`qa/fixtures/t5.1/post-t51-sources/14164e54ec4250e50b915c29c959b35a56ff7ccd249a99c4ad026925af2d8c07.json`

Ownership del freeze y registro/certificación de `CONTENT_MIGRATION_ROUTES`: coordinación/integración.

Este workstream no crea ese fixture ni registra la ruta.

## Validación

Última suite focal completamente validada antes de añadir el guard condicional C012:

- Actions `35236203406` — **SUCCESS** con 9 guards.

Repository integrity correspondiente:

- Actions `35236203349` — falla únicamente en `freeze-t51-active-source --check` porque `14164e54…8c07` todavía no está congelado;
- antes del sentinel pasan build/final gate, T5.2, sport/football moments, saves, T5.3, registries y offer-bridge evidence.

El workflow focal actual añade un décimo guard: `scripts/test-t51-30-34-conditional-seed-reads.mjs`. Su run debe sustituir a estos IDs cuando finalice.

## Próximos frentes seguros

- completar clasificación/metadata condicional solo después de freeze + decisión de identidad;
- continuar sport authority scene-by-scene sin proxies;
- preparar integración multi-oferta;
- preparar cláusula contractual por minutos;
- batch coordinado de cinco missing;
- resolver 18 identidades desplazadas con migraciones explícitas;
- no cerrar seeds downstream sin consumidor/terminal canónico demostrado.

## Estado

El workstream está técnicamente avanzado y con deuda caracterizada, pero **no es merge-ready**.

PR #13 debe permanecer **DRAFT — DO NOT MERGE** hasta resolver el freeze/route actual y los blockers compartidos requeridos por integración.
