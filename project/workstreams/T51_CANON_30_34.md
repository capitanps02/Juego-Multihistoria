# T5.1 — Canon 30–34

## Ownership

Rama exclusiva: `t51/canon-30-34`.

El Documento Maestro prevalece. Este workstream no modifica `main` directamente, no hace merge automático, no crea el freeze post-T5.1 y no registra rutas globales de migración.

## Baseline vigente

Re-ground actual:

`main@5f4d14bca4d696cfafadb58b64034c7cd40cc147`

La generación upstream H está congelada y registrada como:

`84871fae2bec92d74d1e607e0a48943e2e530a062d315a829cfe75eda9fe0886`

El catálogo 30–34 superpuesto produce:

`9151d6620739f5f63face23f412abdeca9898cac8e510cc44d86c1468da8d0f4`

Inventario estricto 30–34:

- 50 principales canónicos;
- 27 IDs estables con semántica reimplementada, todavía no `verified_same_identity`;
- 18 identidades desplazadas con migración explícita pendiente;
- 5 principales canónicos missing;
- 5 engine-only activos;
- 0 aliases aprobados;
- 26 condicionales del motor aún sin inventario condicional canónico autoritativo.

## Autoridad de carrera

Los outcomes narrativos no pueden escribir directamente club/owner/registration/tier/route/prestige, `contract.*` ni flags de transferencia/préstamo/gran club.

Bridges formales activos:

- `EVT_31_HOME_001`;
- `EVT_32_HOME_001`;
- `EVT_32_CON_001`.

`EVT_31_MKT_001` queda fuera: el canon compara dos ofertas y la autoridad actual persiste una sola `CareerOffer` pendiente. La API `getEligibleCareerOffers()` no resuelve este blocker: deriva de `MarketState.pending`, por lo que la cardinalidad persistible sigue siendo uno.

`EVT_33_MKT_001` conserva el mismo blocker para sus cuatro rutas/propuestas.

`EVT_32_CON_001` tampoco puede certificar todavía la cláusula automática por minutos porque `CareerTerms` no la modela.

La pasada de convergencia raw-source eliminó 17 escrituras directas a club/contrato de las definiciones 30–34 que el guard ya descartaba en runtime. Se preservó el orden de tags de los offer bridges y la comparación determinista posterior dio `DIFFERING_EVENTS=0`; por tanto el catálogo activo y `contentIdentity` siguen siendo exactamente los mismos.

## Autoridad de liderazgo

`EVT_33_CAP_001` consume únicamente la autoridad explícita de liderazgo del jugador en el club actual. Influencia, seeds, `captain_group`, `secondary_captain` o liderazgo de un club anterior no desbloquean la escena.

`EVT_30_CAP_001` sigue parcialmente bloqueado: el canon pide un `LOCKER_WEIGHT` alto y aún no existe un umbral compartido autoritativo que permita traducirlo sin inventar semántica. Un ratchet obliga a revisar este blocker si upstream incorpora autoridad de liderazgo específica para `30_34`.

## Autoridad deportiva

Los hechos concretos salen únicamente de `facts.sport` / `facts.match`. Si competición, fixture, convocatoria, titularidad, minutos, resultado o producción deportiva están `null`, el evento falla cerrado.

Reclasificados tras el match model semanal de `main@5f4d14bc…`:

- `EVT_31_FINAL_001`: guard parcial; una fixture de liga no prueba una final;
- `EVT_33_BODY_001`: guard parcial; una distancia numérica no prueba dos titularidades dentro de 72 h.

Pendientes de hechos exactos: `EVT_30_FORM_001`, `EVT_31_RETURN_001`, `EVT_31_ROLE_001`, `EVT_32_FAN_001`, `EVT_32_NAT_001`.

La deuda scene-level mantiene 11 blockers exactos y sin duplicados. El ratchet C005 exige además que esos cinco blockers deportivos permanezcan explícitos; que los cuatro eventos existentes sigan `technical_adaptation`; que `EVT_31_ROLE_001` continúe como adición coordinada ausente; y que ni forma, roleScore, nationalStanding, roleSecurity ni flags puedan sintetizar competición, fixture, convocatoria, banquillo, titularidad, aparición, minutos, resultado o goles. La proyección debe seguir read-only.

No se añaden gates nuevos a esas cinco escenas antes del freeze/edge actual porque moverían `contentIdentity`; cualquier implementación posterior pertenece a un nuevo batch coordinado.

## Seeds y memoria causal

Owner 30–34:

- 52 seeds runtime asignadas;
- 48 con productor runtime;
- 4 sin productor;
- 13 con algún consumidor estructural;
- 39 sin consumidor;
- 0 terminales explícitos;
- 52 open-ended.

Cuatro writers canónicos missing están fijados uno-a-uno:

- `SEED_ROLE_COMMUNICATION` → `EVT_30_CCH_001`;
- `SEED_FALSE_ULTIMATUM` → `EVT_30_PRS_001`;
- `SEED_NATIONAL_ABSENCE` → `EVT_30_NAT_002`;
- `SEED_SPECIALIST_BIGCLUB` → `EVT_30_JAN_001`.

No se sustituyen por productores legacy/genéricos.

### Referencia canónica ausente — C016/C017

El Documento Maestro de `EVT_30_CON_001` referencia `SEED_LAST_PEAK_CONTRACT`, pero ese ID no existe en el catálogo runtime actual. No se crea, renombra ni equipara por semejanza a `SEED_AGE30_CONTRACT`, `SEED_LAST_BIG_MOVE_WINDOW` u otra seed existente.

Esta referencia no se suma artificialmente a las 52 seeds owner 30–34. Su materialización queda bloqueada hasta que T5.2/canon defina identidad, scope/expiración, payload, productores y cualquier consumidor/terminalidad con evidencia real.

### Handoff 34+ / retirada

`analysis/T5.1/canon-30-34-downstream-seed-handoff.json` conserva writers reales de seis seeds frontera sin imponer consumidor ni terminalidad. `SEED_AGE34_PRIORITY` tiene tres writers runtime y `SEED_HOME_PULL_PUBLIC` dos.

### `seedsRead` != causalidad

Siete enlaces principal→principal quedaron degradados a `declaredReadLinks`: declarar `seedsRead` no demuestra dependencia runtime.

Control positivo: `SEED_CHRONIC_BODY → EVT_31_MED_001` sí consume mediante `HAS_SEED_CHRONIC_BODY`.

### Siete callbacks condicionales

Los siete `declaredReadMismatches` owner 30–34 sí son consumidores runtime positivos mediante `HAS_SEED_*`, pero no se promueven a consumidores canónicos mientras sus condicionales sigan sin identidad autoritativa.

Añadir `seedsRead` ahora movería `contentIdentity`; esa metadata queda diferida hasta después del freeze/edge actual y de la decisión canónica condicional.

El artefacto `canon-30-34-conditional-seed-read-debt.json` ya está re-anclado al baseline `main@5f4d14bc…` y target `9151d662…`; su test exige que permanezca alineado con readiness + migration handoff.

## Missing y shifted

Missing coordinados:

1. `EVT_30_CCH_001`;
2. `EVT_30_PRS_001`;
3. `EVT_30_NAT_002`;
4. `EVT_30_JAN_001`;
5. `EVT_31_ROLE_001`.

Las 18 parejas shifted siguen `sameSceneMigrationAllowed:false`. `EVT_31_TEAM_001` mantiene una colisión semántica doble y no se resuelve por string matching.

## Retirada

- edad no fuerza retirada;
- retirada internacional != retirada de club;
- memoria de retirada no es estado terminal;
- `EVT_33_RET_001` sigue siendo adaptación técnica voluntaria, no auto-retirada por edad.

## Migración actual

Fuente upstream congelada:

`84871fae2bec92d74d1e607e0a48943e2e530a062d315a829cfe75eda9fe0886`

Target 30–34:

`9151d6620739f5f63face23f412abdeca9898cac8e510cc44d86c1468da8d0f4`

Freeze pendiente, propiedad de coordinación/integración:

`qa/fixtures/t5.1/post-t51-sources/9151d6620739f5f63face23f412abdeca9898cac8e510cc44d86c1468da8d0f4.json`

Solo después de existir ese fixture puede coordinación/integración revisar y registrar el edge `84871fae… → 9151d662…`.

## Validación exacta

Último HEAD de runtime/guards validado:

`8df83a37b3057dca8cc2321f0f5b7da6f13a4714`

- T5.1 canon 30–34 run `35263497939` — **FAILURE aislada en nomenclatura documental C006; runtime/C005 pasan**;
- T5 Market Contract Authority run `35263497903` — **SUCCESS**;
- Repository Integrity run `35263497858` — **FAILURE únicamente esperada** en `freeze-t51-active-source --check` por ausencia del fixture target `9151d662…8d0f4`.

En `35263497858`, `npm test` completó correctamente build, T5.2, saves, T5.3, registries y offer bridges antes del sentinel. No hay un fallo adicional de manifests ni runtime previo al freeze.

Al cerrar esa validación, `main` seguía en `fa3c8bae…`, la rama estaba `behind_by=0` y el merge-base era el `main` actual.

## Estado

La rama está re-grounded sobre `main@5f4d14bc…`; el catálogo activo conserva el target `9151d662…`; las pruebas focales y de autoridad de mercado están verdes. **No es merge-ready** hasta que coordinación/integración congele ese target, registre la ruta aprobada y se resuelvan los blockers compartidos que sean requisito de integración.

PR #13 permanece **DRAFT — DO NOT MERGE**.
