# T5.11 — segundo lote canónico 23–26

Rama: `integration/t511-23-26`  
Base final de re-ground: `main@42c32b655473ab2bf420cd95b39412ef28880078`  
Fecha: 16/09/2026

## Alcance

T5.11 reemplaza tres shells de 23 años por escenas canónicas verificadas contra `analysis/T5.1/canon-23-26-reimplementation-cards.json` y la readiness del bloque:

- `EVT_23_MONEY_001` — **El asesor de tu padre**;
- `EVT_23_HOME_001` — **Tu nombre en Cerro Alto**;
- `EVT_23_EUR_001` — **La lista continental**.

No se altera contenido 20–23, 30–34 o 34+, ni los trackers protegidos. El re-ground final hereda de `main` la prioridad de transición de fase, el hecho causal read-only `facts.clubWantsRenewal`, T5-QA-018 de procedencia contractual del offer bridge, el hardening de contactos T5.3 y la reconciliación provenance-safe de conocimiento NPC histórico. T5.11 no modifica esas superficies y conserva sus contratos.

## Triggers

Los umbrales siguientes son adaptaciones técnicas de reachability/balance, no cifras canónicas del Documento Maestro:

- MONEY: `professional.moneyComfort >= 36` OR `HAS_SEED_FIRST_BIG_MONEY`;
- HOME: `professional.publicMyth >= 28` OR `professional.homePull >= 32` OR `HAS_SEED_HOME_DISTANCE`;
- EUR: `CONTINENTAL_CONTEXT=true` AND `professional.roleSecurity <= 70`.

MONEY registra estructura y riesgo familiar sin garantizar rentabilidad. HOME registra aceptar o evitar el símbolo formal de UDV. EUR conserva incertidumbre de inscripción y puede registrar intención de salida sin mutar `club`, `contract` o `market.pending`.

## Lineage y saves

Fuente T5.10 (C): `fee2ff875bac7979d3907f5ee1004ef736efa9a257237c6dee629dd8687fe136`.

Destino T5.11 (D): `5d3fd71a8df42ed5b93fdde63ed386e9d776693addd62a30293dbecad7aa56d2`.

La única arista nueva es C → D con tres mappings `distinct_scene` exact-ID para MONEY, HOME y EUR. Los tres liberan `SEEN` y cooldown canónicos. No existen `seedOriginMappings`: history, provenance, pending legacy, `SeedInstance.originEvent` y RNG se preservan.

La evidencia D está congelada en `qa/fixtures/t5.1/post-t51-sources/5d3fd71a8df42ed5b93fdde63ed386e9d776693addd62a30293dbecad7aa56d2.json`.

T5-QA-018 genera además `src/session/frozen-offer-bridge-evidence.ts` desde todas las generaciones congeladas. D se incorpora como cuarta fuente histórica con mapa de bridges vacío, porque estas tres escenas no son offer bridges. Así una futura migración reconoce la identity D sin fabricar semántica contractual retrospectiva.

## QA

`scripts/test-t51-t511-23-26.mjs` verifica canon, reachability, seeds, ausencia de mutaciones contractuales, identity D, lineage C→D, limpieza de `SEEN`/cooldown y preservación de history/provenance/orígenes/RNG. El test queda incorporado permanentemente a `npm test`, `qa:t5:saves`, `qa:t5`, `test:t51` y `test:t51:migration`, además de `test:t51:t511`.

La primera CI de PR #80 descubrió dos expectativas históricas obsoletas en `scripts/test-t51-t510-23-26.mjs`: asumían que el catálogo C debía seguir activo después de existir D. El fix no cambia runtime ni relaja T5.10. Ahora T5.10 certifica C mediante su fixture congelado, exige PRE → B1a → C y exige que snapshots antiguos migren **a través de C** hasta el catálogo activo sin drift de RNG. Esto hace el gate histórico válido para T5.11 y batches posteriores.

El re-ground sobre `42c32b65…` conserva T5-QA-018 y los dos hardenings T5.3. `generate-t51-offer-bridge-evidence.mjs --check`, `test-t51-offer-bridge-provenance.mjs`, `test-t53-contacts.mjs` y `test-t53-reconciliation.mjs` permanecen en sus suites originales, mientras T5.11 añade sus regresiones sin sustituirlas.

## Dependencias fuera del lote

`EVT_23_LOCK_001` sigue bloqueado porque su trigger exige afinidad con `SLOT_CAPTAIN/STAR` OR `SEED_TEAMMATE_COVER` y no existe resolución dinámica canónica de esos slots. MKT/CON permanecen bajo autoridad de ofertas/contratos y `EVT_24_MATCH_001` bajo resolución deportiva separada.

## Cierre

T5.11 solo queda listo para integración si la rama está 0-behind, no hay workflow temporal de freeze en el diff, fixture/registries son reproducibles, `T51 content migration` y `Repository integrity` pasan completos, los gates de choice eligibility, offer/session authority, offer-bridge provenance y T5.3 no regresan y la PR es mergeable.

Este workstream no ejecuta merge automático.
