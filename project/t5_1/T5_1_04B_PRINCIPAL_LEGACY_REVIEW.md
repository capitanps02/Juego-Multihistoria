# T5.1 — Batch 04B semantic review of age-35 runtime-only principal IDs

Generated: 2026-09-16  
Branch: `chore/chatgpt-codex-workflow`

## Result

Reviewed age-35 runtime-only IDs: **10/10**.  
Approved same-scene migrations: **0/10**.  
Disposition for all ten: `retire_technical_keep_history_only`.

## Structural evidence

Every reviewed legacy ID is built by the common `generic(...)` late-career adapter. The runtime therefore gives unrelated themes the same broad body, the same role/body/contract intelligence, the same four choices and generic numeric effects. The canonical age-35 scenes instead encode concrete factual situations and scene-specific memories.

Representative differences:

- `EVT_35_CON_001` is a physical-availability incentive contract, not a generic option/renewal scene.
- `EVT_35_FAREWELL_001` uses a proposed homage as institutional leverage around an expiring contract.
- `EVT_35_DUAL_001` asks whether to become player + institutional liaison.
- `EVT_35_TACT_001` is caused by a successful late positional reinvention.
- `EVT_35_BENCH_001` requires five consecutive unused matches and presents a concrete wait/rescind/press/mentor decision.
- `EVT_35_JAN_001` is a real six-month January offer under a reduced role and preserves parallel-negotiation history.

None of those facts are represented by the common generic A/B/C/D runtime contract.

## Dispositions

- `EVT_35_PRE_001`: retire history only; no unique canonical same-scene target.
- `EVT_35_OPT_001`: broad contract lineage only; not `EVT_35_CON_001`.
- `EVT_35_ROLE_001`: reduced-role lineage only; not `EVT_35_BENCH_001`.
- `EVT_35_NAT_001`: national-team theme only; not canonical emergency/cycle scene.
- `EVT_35_MENT_001`: mentor/transition theme only; not canonical dual institutional role.
- `EVT_35_MED_001`: physical-risk lineage only; not the canonical availability-contract decision.
- `EVT_35_PRS_001`: farewell/public-pressure theme only; not the homage-as-leverage contract scene.
- `EVT_35_END_001`: generic June continuation; cannot become a hidden retirement decision.
- `EVT_35_BUS_001`: commercial/image lineage only; not a canonical sponsorship decision.
- `EVT_35_CAP_001`: generic captaincy/role adaptation with no unique canonical same-scene target.

## Migration implications

Completed history remains under each legacy ID. Pending legacy content retains its existing choice contract via compatibility or fails explicitly; it is never replaced by a canonical scene with different choices. Canonical `SEEN_*` and seed origins are not manufactured from thematic similarity. Compatibility-only legacy definitions never return to active scheduling.

## Status

**04B_LEGACY_REVIEW_COMPLETE — 10/10 RETIRE_HISTORY_ONLY — 0 SAME-SCENE MIGRATIONS**
