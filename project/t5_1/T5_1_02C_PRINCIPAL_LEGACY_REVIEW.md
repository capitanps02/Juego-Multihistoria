# T5.1 — Batch 02C semantic review of age-27 runtime-only principal IDs

Generated: 2026-09-16  
Branch reviewed: `chore/chatgpt-codex-workflow`

## Scope

Review the four runtime-only age-27 principal IDs assigned to Batch 02C against the seven remaining missing canonical age-27 scenes after PR #5 owns `EVT_27_STAR_001` and `EVT_27_AWARD_001`.

Runtime-only IDs:
- `EVT_27_REC_001` — El récord empieza a estar cerca
- `EVT_27_RIV_001` — Otra vez comparados
- `EVT_27_MENT_001` — ¿Qué le dirías al de 19?
- `EVT_27_FINAL_001` — La final empieza en el banquillo

Canonical 02C targets:
- `EVT_27_CON_001` — La cláusula que sí puede pagarse
- `EVT_27_IMG_001` — Tu nombre sin tu club
- `EVT_27_HOME_001` — ¿Comprar una parte de casa?
- `EVT_27_EUR_001` — La semifinal que exige desaparecer
- `EVT_27_MATCH_001` — El récord no se celebra
- `EVT_27_TEAM_001` — Tu protegido juega por ti
- `EVT_27_AGT_002` — Tu agente negocia dos futuros

All runtime rows are generated by the generic 26–30 factory, so title/seed/theme can show lineage but cannot establish scene identity.

## Executive result

Reviewed runtime-only IDs: **4/4**.  
Approved same-scene migrations: **0/4**.

Disposition for all four:

`retire_technical_keep_history_only`

For all four:
- old history ID rewrite: not allowed;
- pending direct canonical substitution: not allowed;
- canonical `SEEN_*` manufacturing: not allowed;
- legacy compatibility definition, if supported, must stay outside active scheduling.

PR #7 may remove false seed-origin behavior before 02C runs, but that chronology repair does not convert these legacy scenes into canonical scenes.

---

## `EVT_27_REC_001` — El récord empieza a estar cerca

Runtime role:
- age 27 generic `legacy` event;
- currently creates `SEED_RECORD_CHASE`;
- generic peak-era body/intel/choices.

Canonical chronology:
- `SEED_RECORD_CHASE` canonically begins in age-26 `EVT_26_MATCH_001` — El penalti para un récord;
- canonical age-27 `EVT_27_MATCH_001` — El récord no se celebra — requires the record chase to have resolved and then asks how to handle a record achieved in a painful team result;
- `EVT_27_MATCH_001` creates `SEED_RECORD_PUBLIC_TONE`, not `SEED_RECORD_CHASE`.

Decision:
- `EVT_27_REC_001` is an obsolete technical precursor for record chronology;
- it is **not** the canonical age-27 record scene;
- retire after PR #7 has moved `SEED_RECORD_CHASE` origin to age 26 and 02C implements `EVT_27_MATCH_001`.

Disposition: `retire_technical_keep_history_only`.

---

## `EVT_27_RIV_001` — Otra vez comparados

Runtime role:
- age 27 generic press event;
- creates `SEED_PUBLIC_RIVALRY`;
- reads `SEED_ADRIAN_MIRROR`;
- generic peak-era body/intel/choices.

Canonical chronology:
- age-26 canonical `EVT_26_RIV_001` — Adrián en el otro vestuario — already owns the return of the Adrián comparison and creates `SEED_PUBLIC_RIVALRY`;
- none of the seven 02C canonical targets is the same Adrián-comparison scene.

Decision:
- strong causal/theme lineage to the canonical age-26 Rivas/Adrián chain, but wrong canonical age/responsibility and generic scene contract;
- PR #7 moves the seed origin to `EVT_26_RIV_001`;
- 02C can then retire `EVT_27_RIV_001` from active principals without relabeling its old history.

Disposition: `retire_technical_keep_history_only`.

---

## `EVT_27_MENT_001` — ¿Qué le dirías al de 19?

Runtime role:
- age 27 generic team event;
- creates `SEED_MENTOR_ADVICE`;
- generic peak-era body/intel/choices.

Canonical chronology:
- age-26 canonical `EVT_26_TEAM_002` — El sustituto te pregunta si debe irse — owns `SEED_MENTOR_ADVICE`;
- canonical age-27 `EVT_27_TEAM_001` — Tu protegido juega por ti — is a different later consequence: the young player performs well in the protagonist's absence and may keep the position;
- canonical choices concern support, competition, tactical coexistence and pressure to return, not advice about whether the youngster should leave.

Decision:
- causal predecessor concept only, not same scene;
- PR #7 moves `SEED_MENTOR_ADVICE` origin to age 26;
- 02C implements the distinct `EVT_27_TEAM_001` consequence.

Disposition: `retire_technical_keep_history_only`.

---

## `EVT_27_FINAL_001` — La final empieza en el banquillo

Runtime role:
- age 27 generic sport event;
- final-context gate + lower role-score gate;
- currently creates `SEED_BIG_GAME_BENCH`;
- marked `verified:true` but generated by the same generic factory.

Canonical chronology:
- age-26 `EVT_26_EUR_001` — La noche en la que no empiezas — canonically creates `SEED_BIG_GAME_BENCH`;
- age-26 `EVT_26_FINAL_001` — La final y el banquillo — consumes that context and creates `SEED_FINAL_BENCH`;
- canonical age-27 `EVT_27_EUR_001` — La semifinal que exige desaparecer — is instead a sacrifice-role dilemma where tactical pressing/help reduces individual visibility during an award cycle and creates `SEED_ELITE_SACRIFICE`.

Decision:
- runtime `EVT_27_FINAL_001` is neither canonical `EVT_26_FINAL_001` nor canonical `EVT_27_EUR_001`;
- its `verified:true` status does not change that;
- PR #7 removes its false `SEED_BIG_GAME_BENCH` origin responsibility;
- 02C retires the technical event after the age-26 final chain is canonical and age-27 semifinal scene exists.

Disposition: `retire_technical_keep_history_only`.

## Migration implications

### Completed history

Keep all four legacy IDs exactly as played. They remain evidence of old technical content, not proof that their canonical successor/predecessor scenes occurred.

### Pending decisions

Do not rewrite pending legacy IDs to:
- `EVT_26_MATCH_001` / `EVT_27_MATCH_001`;
- `EVT_26_RIV_001`;
- `EVT_26_TEAM_002` / `EVT_27_TEAM_001`;
- `EVT_26_EUR_001` / `EVT_26_FINAL_001` / `EVT_27_EUR_001`.

Resolve the exact embedded legacy definition through supported content compatibility or fail explicitly.

### Seeds

Moving future catalog origins in PR #7 must not rewrite historical `seed.originEvent` automatically. A historical seed created by one of these legacy events may remain a valid memory state, but it is not evidence the canonical origin scene was played.

### Active scheduling

After PR #7 chronology repair and 02C canonical implementation:
- these four legacy IDs should no longer appear in active principal scheduling;
- no compatibility-only definition may enter `EVENTS` / `EventIndex`.

## Status

**02C_LEGACY_REVIEW_COMPLETE — 4/4 RETIRE_HISTORY_ONLY — 0 SAME-SCENE MIGRATIONS**
